<li>
	<span class="name"><%= ingredient.name %></span>
	<span class="quantity"><%= ingredient.quantity %></span>
	<span class="unit"><%= ingredient.unit %></span>
	<% if current_user == ingredient.recipe.user %>
		<%= link_to "delete", ingredient, method: :delete %>
	<% end %>
</li>